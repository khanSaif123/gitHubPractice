import { calculateWeight } from "../saif";

let newFeatures = "This is the new feature";
function newFeature(){
    return newFeatures;
}

let handleLogs = () => {
    console.log("Error is there in the new feature");
}

// here one new function required.
calculateWeight(57);
